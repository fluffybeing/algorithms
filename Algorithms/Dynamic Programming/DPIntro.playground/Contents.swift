import Cocoa

// Optimal Substructure
// Overlapping subproblems

// top-down: recursive because we are getting the last value and
// breaking it down to the smaller on recursively

// FAST => First Solution, Analyze subproblems, subproblems, turn the solution around


